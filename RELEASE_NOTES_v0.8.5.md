# Free-Model-Router v0.8.5

## 주요 변경 사항

- OpenRouter의 무료 모델과 OpenCode/NVIDIA 모델 목록을 더 안정적으로 불러옵니다.
- 모델 응답이 처음 시작되는 시간(TTFT)을 더 정확하게 측정하고, 첫 응답을 확인하면 불필요한 probe 연결을 바로 종료합니다.
- 일시적으로 응답하지 않는 모델은 자동 확인 간격을 늘려 불필요한 요청과 무료 할당량 소모를 줄입니다.
- 최신 측정값은 라우팅에 사용하고, 오래된 측정값은 `stale` 표시로 구분해 화면에서만 확인할 수 있습니다.
- 모델 목록을 30분마다 자동으로 다시 불러와, 재시작하지 않아도 새 모델과 변경 사항을 반영합니다.

## 설치 파일

- 일반 설치: NSIS 설치 파일
- 기업·관리자 배포: MSI 설치 파일
- 압축 파일: 실행 파일과 필요한 구성 요소가 포함된 ZIP 파일
