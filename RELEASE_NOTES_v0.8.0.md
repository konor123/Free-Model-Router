# Free-Model-Router v0.8.0

이번 버전에서는 데스크톱 앱의 설정과 모델 관리 흐름을 한 창으로 통합했습니다.

## 주요 변경 사항

- `Provider`, `Model`, `Usage log`, `Endpoint/key` 탭을 하나의 메인 창에서 사용할 수 있습니다.
- OpenAI 호환 Provider를 추가·수정·비활성화하고 API 키를 안전하게 교체할 수 있습니다.
- API 키는 설정 파일이나 제어 API 응답에 노출되지 않고 운영체제 자격 증명 저장소에 보관됩니다.
- Provider 모델 목록을 `🔃 Rediscover` 버튼으로 다시 불러올 수 있습니다.
- 모델 성능, 지연 시간, 라우팅 점수를 정렬할 수 있으며 확인되지 않은 값은 `-`로 표시됩니다.
- 데스크톱이 시작한 게이트웨이는 설정 저장 후 자동으로 재시작됩니다. 외부 게이트웨이는 재시작 필요 여부를 안내합니다.
- 트레이 메뉴를 Windows 자동 시작과 종료 항목으로 단순화했습니다. 트레이 아이콘을 왼쪽 클릭하면 메인 창이 열립니다.
- 마지막으로 성공한 모델을 트레이 도움말에서 확인할 수 있습니다.

## 보안 안내

- 관리 API는 localhost 전용이며 모든 요청에 관리 토큰이 필요합니다.
- 원격 Provider는 HTTPS만 허용합니다. HTTP Provider는 loopback 주소에서만 사용할 수 있습니다.
- loopback 추론 주소는 기존처럼 인증을 강제하지 않습니다. LAN에 공개하는 경우 별도 추론 키가 필요합니다.

클린 Windows VM에서의 설치·업그레이드·제거 검증은 환경별로 별도 확인이 필요합니다.
