## Goal

OpenEvals benchmark 수집 결과를 안전하게 매칭·보존하고 runtime scoring에 적용한다.

## Actions

- unique benchmark matcher를 추가해 동일 강도 후보가 있으면 자동 binding하지 않게 했다.
- OpenEvals snapshot에 revision, upstream version/update time, policy version provenance를 추가했다.
- revision-pinned OpenEvals fetch/cache lifecycle과 percentile mapping 검증을 통과시켰다.

## Files Changed

- `internal/matcher/matcher.go`: unique binding API.
- `internal/matcher/matcher_test.go`: ambiguous alias regression test.
- `internal/gateway/performance.go`: unique binding 적용.
- `internal/scoring/cache.go`: snapshot provenance.
- `internal/benchmarks/openevals/client.go`: upstream provenance 설정.

## Result

- success
- `go test ./internal/matcher ./internal/scoring ./internal/gateway ./internal/benchmarks/openevals -count=1` 통과.

## Next Steps

- 경로별 TTFT의 probe/request 관측 상태와 UI/control 상태를 마무리한다.