## Goal

Hugging Face OpenEvals leaderboard를 실제 benchmark snapshot으로 수집하고 application lifecycle에 연결한다.

## Actions

- revision-pinned Hugging Face metadata/leaderboard JSON fetcher를 추가했다.
- coding·agent와 math·knowledge benchmark만 percentile로 정규화했다.
- Availability를 nil로 유지하고 aggregateScore/OCR 점수를 routing Performance에 사용하지 않았다.
- OpenEvals cache를 startup에 복원하고 즉시 및 24시간 주기 background refresh를 연결했다.
- cache 저장 실패나 network 실패 시 기존 in-memory snapshot을 유지하도록 했다.

## Files Changed

- `internal/benchmarks/openevals/client.go`: OpenEvals fetch/normalization 구현.
- `internal/benchmarks/openevals/client_test.go`: sparse, OCR 제외, duplicate/range 검증 테스트.
- `internal/app/benchmark.go`: cache bootstrap 및 background refresh.
- `internal/app/app.go`: OpenEvals cache lifecycle 연결.

## Result

- success
- `go test ./internal/app ./internal/benchmarks/openevals -count=1` 통과.

## Next Steps

- snapshot provenance/UI 상태와 안전한 matcher 적용을 보완한다.
- 경로별 TTFT 관측·freshness·control/UI polling을 구현한다.
