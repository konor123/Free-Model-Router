## Goal

v0.8.0 GitHub Actions Windows release 실패를 수정한다.

## Actions

- 실패 로그에서 Tauri cargo test가 bundle resource 파일 생성 전에 실행된 것을 확인했다.
- gateway sidecar, metadata, fmr CLI 준비 단계를 cargo test 앞으로 이동했다.
- YAML 파싱과 step ordering/resource command를 검증했다.
- Tester와 reviewer가 해당 CI 실패 원인이 해결되었음을 확인했다.

## Files Changed

- `.github/workflows/release-windows.yml`: bundled resource 준비 순서 수정.

## Result

- 성공.
- 기존 실패 원인 `resource path ... does not exist`가 해결되었다.
- 로컬 actionlint는 설치되지 않았지만 YAML 파싱과 명령/순서 검증은 통과했다.

## Next Steps

- workflow 수정 커밋을 main에 push한다.
- workflow_dispatch로 Windows 패키지 작업을 재실행해 성공을 확인한다.
- v0.8.0 GitHub Release에 검증된 자산을 게시한다.
