# Orchestrator Log — 2026-09-09 14:00

## Goal
OpenCode Zen 유료 모델의 익명 무료 카탈로그 유입을 차단하고, Desktop managed gateway의 CMD 창 표시를 제거한다.

## Actions
- 공식 가격 문서 기준 6개 OpenCode 무료 모델 exact allowlist를 추가했다.
- native OpenCode catalog를 해당 allowlist로만 필터링하고, generic OpenAI-compatible adapter에는 영향을 주지 않도록 분리했다.
- Desktop 저장 Zen key를 instance-local resolver로 native OpenCode provider에 주입해 Public과 Zen 무료 route를 함께 만들도록 변경했다.
- native OpenCode의 Zen 요청은 요청 직전에 key를 조회하고, public 요청에는 credential을 보내지 않도록 유지했다.
- native OpenCode HTTP client의 redirect를 차단했다.
- managed gateway child process에 Windows CREATE_NO_WINDOW flag를 적용했다.
- Start with Windows를 HKCU Run registry 값으로 전환하고 기존 CMD launcher를 제거하도록 변경했다.
- README에 OpenCode 무료 catalog fail-closed 정책을 기록했다.

## Files Changed
- `internal/providers/opencode/free_models.go`: 공식 확인 무료 ID 정책.
- `internal/providers/opencode/{provider.go,route.go,auth_route.go,chat.go}`: native catalog/route/credential 정책.
- `internal/app/providers.go`: stored-key native OpenCode wiring.
- `desktop/src-tauri/{Cargo.toml,Cargo.lock,src/main.rs}`: Windows child-process 및 autostart 처리.
- `README.md`: OpenCode catalog 정책.

## Result
- 부분 성공.
- focused Go tests와 Rust desktop tests가 통과했다.
- 전체 test/lint/package 검증 및 독립 tester/reviewer 검토가 남아 있다.

## Next Steps
- 전체 Go/Rust/frontend 검증을 실행한다.
- 실패가 있으면 원인을 수정한다.
- tester/reviewer 검토 후 최종 로그를 남긴다.
