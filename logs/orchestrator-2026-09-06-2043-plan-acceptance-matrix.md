# PLAN_V7.1 Acceptance Matrix and Whole-Result Feedback Loop

- Date: 2026-09-06 UTC validation sweep
- Repository: `Free-Model-Router`
- Purpose: close the feedback loop over the complete PLAN_V7.1 result, map every explicit phase gate and changed public boundary to observed evidence, and record blockers without overstating completion.
- Pre-log HEAD: `c62789689000cc5a9970f0a301d91fcd4bb2fe72`
- Pre-log worktree: only the pre-existing untracked `.codegraph/` directory. It was not staged or modified.

## Evidence legend

- **PASS** means a real command or project test executed and returned exit code 0 with the named behavior observed.
- **ACCEPTANCE-BLOCKED** means the project path was identified but the required external infrastructure was unavailable. These items are not claimed as passed.
- **COMMAND-CORRECTED** means an earlier wrapper invocation was invalid on Windows `cmd.exe`; the equivalent check was rerun with valid syntax before drawing conclusions.

## Phase-to-evidence trace

| Phase | Explicit requirements and public output | Concrete check and observed result |
|---|---|---|
| 0 Bootstrap | Go module, config, logging, CI/build scripts, dependency direction, buildable CLI | `go test -count=1 ./...`, `go vet ./...`, and `go build ./cmd/fmr` all **PASS**. The command help path `go run ./cmd/fmr --help` **PASS** and printed `start`, `stop`, `status`, `models`, `providers`, `model-pool`, `config`, `logs`, and `doctor`. |
| 1 Identity/core types | `CanonicalModelKey`, `ProviderModelID`, `RouteID`, access/capability/request requirements, failure class/scope/retryability, cancellation | `go test -count=1 ./internal/model/... ./internal/provider/...` **PASS**. Named tests **PASS**: `TestSameCanonicalDifferentProviderDifferentID`, `TestSameProviderDifferentVariantDifferentID`, `TestSameProviderModelDifferentRouteDifferentRouteID`, `TestCanceledFailureForbidsFallback`, `TestFailureDecisionScopeAndClassMatrix`, and identity/path validation tests. |
| 2 Single-provider E2E | OpenCode catalog, normalized OpenAI-compatible request, `/v1/models`, `/v1/chat/completions`, streaming, malformed/timeout/cancel behavior, explicit unsupported-parameter errors | `go test -count=1 ./internal/providers/opencode/... ./internal/gateway/...` **PASS**. The real application integration test `TestRunWithProviderWiresRealHTTPAppPath` **PASS** over a TCP listener, exercising `/v1/models` and `/v1/chat/completions` against a local mock upstream. OpenCode named tests **PASS**: `TestDiscoverModelsCatalog`, `TestDiscoverModelsMalformed`, `TestDiscoverModelsServerError`, `TestChatCompletionNonStreaming`, `TestChatCompletionStreamingSSE`, `TestChatCompletionClientCancel`, `TestUnsupportedExtensionExplicitError`, and `TestSSEPreservesToolCallsAndReasoning`. |
| 3 Model pool/catalog | Immutable snapshots, atomic catalog replacement, new/disappeared/returned models, access changes, automatic/manual modes, revision conflict | `go test -count=1 ./internal/catalog/... ./internal/config/...` **PASS**. Named tests **PASS**: `TestNewModelAppearsAutomaticMode`, `TestModelDisappearsTombstonedAndReturnsRestored`, `TestManualModeDoesNotAutoAdd`, `TestSelectSwitchesToManual`, `TestDetectAccessChanges`, `TestRevisionConflictDetected`, and `TestStoreAssignsMonotonicSnapshotRevisionsAndCopiesModels`. |
| 4 Capability filtering | Effective model/route capability intersection, capability mismatch before score, requirements retained during fallback, vision/tools/structured output/context/max output/streaming filtering | The literal `go test -count=1 ./internal/router/... -run Capability` command returned exit 0 but selected no tests because the existing test names do not contain the word `Capability`; this is recorded rather than overstated. The complete `go test -v -count=1 ./internal/router/...` **PASS**, and the named requirement tests **PASS**: `TestVisionExcludesTextOnly`, `TestToolsExcludeNoTools`, `TestStructuredOutputIncompatibleExcluded`, `TestContextOverflowExcluded`, `TestMaxOutputExcluded`, `TestKnownSmallContextExcluded`, `TestKnownSmallMaxOutputExcluded`, `TestStreamingUnsupportedExcluded`, `TestFallbackKeepsSameRequirements`, and `TestProtocolVetoApplied`. |
| 5 OpenCode dual route | Optional API key, Public/Auth routes, route-specific access/status/health/latency/cooldown/quota, auth failure isolation | `go test -count=1 ./internal/providers/opencode/... -run "Route"` **PASS** after Windows-compatible quoting. Named tests **PASS**: `TestAuthAndPublicRoutesTargetDifferentEndpoints`, `TestAuth401DoesNotAffectPublic`, `TestAuthKeyEnvOptional`, `TestNoKeyMeansPublicOnly`, `TestKeyPresentAddsAuthRoute`, `TestRouteAccessIndependence`, and `TestAuthRouteStateIsolationOnFailure`. |
| 6 TTFT/health | First semantic-event TTFT, probe/request/total EWMA, cooldown/quota/health/jitter/concurrency, route-specific state | `go test -count=1 ./internal/latency/... ./internal/health/...` **PASS**. Gateway integration named tests **PASS**: `TestProbeCycleUsesLivePoolAndHTTPProvider`, `TestNonStreamingRecordsTTFTAndTotalMetrics`, and `TestGatewayRanksCandidatesByProbeTTFT`. |
| 7 Failure-aware fallback | Eligibility order, 429 alternate route, unsupported next model, credential scope, 5xx, attempt/budget limits, cancellation no-fallback | `go test -count=1 ./internal/router/...` **PASS**. Named tests **PASS**: `TestFallbackRateLimitPrefersSameModelAlternateRoute`, `TestFallbackUnsupportedSkipsSameModel`, `TestFallbackAuthFailureSkipsCredential`, `TestFallbackProtocolSkipsSameModelAlternate`, `TestFallbackRespectsMaxAttempts`, `TestFallbackBudgetStopsAdditionalAttempts`, `TestCanceledFailureDoesNotFallback`, and failure decision matrix tests. |
| 8 Streaming commit guard | No downstream commit before semantic event, fallback before commit, no fallback after text/tool-call commit, headers-only fallback, disconnect no fallback | `go test -count=1 ./internal/gateway/... -run "Streaming"` **PASS**. Named tests **PASS**: `TestStreamingPreSemanticFailureFallsBack`, `TestStreamingPostTextFailureDoesNotFallback`, `TestStreamingPostToolCallFailureDoesNotFallback`, `TestStreamingHeadersOnlyFailureFallsBack`, `TestStreamingClientDisconnectDoesNotFallback`, `TestStreamingFailureBeforeSemanticDoesNotCommit`, and `TestStreamingFailureAfterSemanticCommitsAndRecordsMetrics`. |
| 9 Performance scoring | Canonical benchmark binding, score/confidence/unknown defaults, cache/LKG behavior, matcher variants, runtime score and TTFT ranking | `go test -count=1 ./internal/matcher/... ./internal/scoring/...` **PASS**. Router/gateway named tests **PASS**: `TestRankWithScorePrefersPerformanceAfterEligibility`, `TestRankWithScoreFallsBackToTTFTWithoutScores`, `TestRankPrefersKnownLowerTTFTAndUsesStableTieBreakers`, `TestRankUsesModelAndRouteIDForUnknownTTFTTies`, and `TestBenchmarkScoreChangesRuntimeCandidateSelection`. |
| 10 Additional providers | NVIDIA, Gemini, xAI each: catalog, identity, route, access, capability, auth, streaming, errors, quota, mocks; no router provider-specific branching | `go test -count=1 ./internal/providers/nvidia/...`, `./internal/providers/gemini/...`, and `./internal/providers/xai/...` each **PASS**. Each suite observed **PASS** for catalog/malformed/server error, non-streaming, streaming, rate limit, auth failure where applicable, cancellation, explicit unsupported extensions, route identity/access, API-key source, and SSE/tool/reasoning preservation. |
| 11 Persistence/secrets | Separate config/secrets/cache/runtime/log state, schema version, atomic write, OS paths, environment > OS credential store > protected file, migration/recovery/redaction | `go test -v -count=1 ./internal/config/...` **PASS**. Named tests **PASS**: `TestSaveLoadRoundTripAtomic`, `TestPersistedConfigSchemaMigration`, `TestLoadCorruptConfigRecoversDefaults`, `TestSecretStorePriority`, `TestSecretStoreDoesNotExposeBackendSecretErrors`, `TestSecretStoreFileFallbackPersistsAtomically`, `TestDefaultStatePathsUseApplicationDirectory`, `TestLoadRejectsFutureSchemaWithoutRecovery`, `TestSecretStoreConcurrentOperations`, `TestPublicPersistenceAndSecretContracts`, `TestAppLoadConfigUsesPersistedConfigContract`, and `TestPublicDefaultLocationsAndSecretEnvironmentOverride`. |
| 12 Usage logging | Request/Attempt separation, route/failure/TTFT/latency/commit/status/tokens, no prompt/response/key/auth header, 20 MB/30 day retention, no duplicate fallback usage | `go test -v -count=1 ./internal/usage/...` **PASS**. Named tests **PASS**: `TestStoreSeparatesRequestAndAttemptsWithoutSensitiveFields`, `TestStorePublishesRedactedCompletionEvent`, `TestStoreRetentionByAgeAndSize`, `TestStorePersistsAndReopens`, `TestQueryFiltersUsageRecords`, and `TestBroadcasterPublishesAndUnsubscribes`. Gateway integration **PASS**: `TestUsageRecordsOneRequestAcrossFallbackAttempts` and `TestUsageRecordsCommittedStreamingFailureAsPartial`. |
| 13 Control API/CLI | Loopback-only management API, bearer token, exact `/_fmr/*` endpoints, IDs in JSON bodies, status handshake fields, optimistic pool revision, logs, all nine CLI commands | `go test -count=1 ./internal/control/... ./cmd/fmr/...` **PASS**. Named tests **PASS**: `TestControlAPIStatusAndBearerAuth`, `TestControlStatusAdvertisesOnlyAvailableFeatures`, `TestControlAPIRejectsNonLoopbackAndKeepsModelIDsInBodies`, `TestControlAPILogsExposeFiltersWithoutRequestContent`, `TestControlSnapshotAndOptimisticPoolMutations`, `TestTypedClientReadsDesktopContracts`, `TestControlLogsEventsStreamsSafeLiveUsage`, `TestFilterModelsSupportsModelManagerFilters`, and `go run ./cmd/fmr --help` **PASS** with all planned command names visible. |
| 14 Security | Localhost inference default, explicit wildcard bind, inference token on LAN, management API always loopback, invalid token rejection, no secrets in logs | `go test -count=1 ./internal/security/...` and `go test -count=1 ./internal/app/...` **PASS**. Actual app-path named tests **PASS**: `TestRunWithProviderExposesAuthenticatedControlBoundary`, `TestRunWithProviderDoesNotLogAuthenticationTokens`, and `TestRunWithProviderRequiresInferenceTokenForWildcardBind`. This exercised the running app and HTTP boundaries, not only helper functions. |
| 15 Tauri tray/lifecycle | Tray/provider snapshot, single instance, stale/live lease handling, attach/start, compatible/incompatible API, external ownership, autostart, managed-only stop | `go test -v -count=1 ./internal/desktop/...` **PASS**. Named tests **PASS**: `TestInstanceLockSecondAcquireAndRelease`, `TestInstanceLockRecoversStaleLease`, `TestInstanceLockNeverStealsLiveLease`, `TestAttachOrStartCompatibleExternal`, `TestAttachOrStartRejectsIncompatibleExternalWithoutStopping`, `TestAttachOrStartManagedStopsOnlyManagedProcess`, `TestTraySnapshotAndProviderMenu`, and `TestFileAutostartEnableDisable`. Native Rust `cargo fmt --check`, `cargo test`, and `cargo clippy --all-targets -- -D warnings` **PASS** with all 7 lifecycle/security tests passing, including no duplicate gateway after authentication failure, compatible-major attach, loopback control address, public ownership JSON, external-process preservation, safe sidecar filename, and atomic autostart. |
| 16 Model Manager | Checkbox pool, bulk/filter/provider selection semantics, search/access/status/capability/performance/TTFT/routing score/routes/pin/auto, revision conflict | Backend `TestFilterModelsSupportsModelManagerFilters` and `TestControlSnapshotAndOptimisticPoolMutations` **PASS**. Frontend `npm test --prefix desktop` **PASS** with `model search keeps its input element while filtering`; `node --check desktop/frontend/app.js` **PASS**. The model-manager filter HTTP contract was exercised; a full interactive GUI automation run is not claimed. |
| 17 Usage Log Window | Time/model/provider/attempt/TTFT/latency/result rows, expansion/fallback chain, time/provider/model/result/fallback filters, live updates, no prompt/response | `TestControlLogsEventsStreamsSafeLiveUsage`, `TestControlAPILogsExposeFiltersWithoutRequestContent`, `TestQueryFiltersUsageRecords`, `TestStorePublishesRedactedCompletionEvent`, and gateway usage integration tests all **PASS** through the control/usage boundaries. Frontend Node regression **PASS**. The live SSE backend path was exercised; full manual multi-window GUI interaction is not claimed. |
| 18 Packaging | Tauri executable + Go sidecar, no Node/npm runtime requirement, CLI binary, Windows-first artifacts, install/first run/autostart/upgrade/uninstall | Portable packaging script plus `fmr-package -validate` and ZIP extraction **PASS**, ending in `NATIVE_PACKAGE_FEEDBACK_OK`. Manifest observed `runtimeRequirements.go/node/npm/rust=false`, `desktopIncluded=true`, target `x86_64-pc-windows-msvc`, API major `1`, SHA-256 metadata, and desktop-managed ownership. Native Windows `cargo tauri build` **PASS**, producing `free-model-router-desktop.exe`, `Free-Model-Router_0.7.1_x64-setup.exe`, and `Free-Model-Router_0.7.1_x64_en-US.msi`. Packaging tests **PASS**: `TestWindowsManifestRoundTripAndRuntimeContract`, `TestWindowsManifestRejectsMismatchedSidecar`, `TestCheckedInWindowsManifestSchemaIsValid`, `TestGenerateWritesValidatedSidecarMetadata`, `TestMetadataRejectsTamperingAndInvalidContracts`, `TestReadRejectsMalformedMetadata`, and `TestRunGeneratesAndValidatesWindowsPackageMetadata`. |

## Common completion conditions

1. **Code build:** Go packages and commands built successfully. Native Tauri Windows release build and NSIS/MSI bundling also succeeded.
2. **Phase unit tests:** Every available phase gate listed above returned exit code 0. The Phase 4 literal selector was a no-test selection, so the complete router suite and named capability tests are the evidence used for that behavior.
3. **Regression:** `go test -count=1 ./...` **PASS** across all Go packages, including `cmd/fmr`, `cmd/fmr-package`, `internal/app`, `internal/catalog`, `internal/config`, `internal/control`, `internal/desktop`, `internal/gateway`, `internal/health`, `internal/latency`, `internal/matcher`, `internal/model`, `internal/packaging`, `internal/providers`, `internal/router`, `internal/scoring`, `internal/security`, and `internal/usage`. `go vet ./...` **PASS**.
4. **Config/schema record:** Config, secret, usage, control, desktop, and packaging schema contracts are represented in code tests and the repository documentation/log chain. This matrix is the whole-result acceptance record.
5. **Documentation:** Root README, `desktop/README.md`, and `packaging/windows/README.md` are present and were included in the reviewed result.
6. **Next public contracts:** Control API, typed client, usage event stream, desktop lifecycle ownership, and package metadata contracts were exercised by integration tests and native tests.
7. **Frontend/native checks:** `npm test --prefix desktop`, `node --check desktop/frontend/app.js`, Rust format, Rust tests, and Rust clippy all **PASS**.

## Failures and corrections retained for auditability

- A combined Phase 14 command initially used Unix single-quote syntax and pipe characters under Windows `cmd.exe`; it exited with a wrapper error and no test result. The security and app commands were rerun as standalone Windows-compatible commands and **PASS**.
- The literal Phase 4 `-run Capability` selector returned exit 0 with no matching tests. The complete router suite and exact named capability tests were then run and **PASS**. No coverage is inferred from the empty selector.
- A prior `GOOS=windows/GOARCH=amd64` wrapper invocation was malformed for the host executor (`unsupported GOOS/GOARCH pair windows /amd64`). This is not treated as a source pass or failure. The actual Windows-targeted native Tauri build and Windows packages subsequently **PASS**.
- `go test -race ./...` remains **ACCEPTANCE-BLOCKED** on this host because the executor reports `-race requires cgo; enable cgo by setting CGO_ENABLED=1`. No race result is claimed.

## External acceptance gates not available in this workspace

The following PLAN_V7.1 Phase 18 gates remain explicitly unverified and require infrastructure beyond this workspace:

- clean Windows VM installation and uninstall;
- first-run behavior on a clean profile;
- persistent OS autostart across reboot/login;
- upgrade preserving user data;
- signed installer/update verification;
- true multi-process desktop/tray race behavior in a clean OS session.

The local native build, package metadata validation, Rust lifecycle tests, stale/live lease tests, and portable ZIP extraction provide representative evidence, but they do not replace those external gates. The implementation is therefore locally workflow-validated with explicit external acceptance deferrals, not globally certified.

## Independent review signal

The successful post-fix independent review runs reported zero findings for their selected scopes. Some later review invocations failed or returned partial executor wrappers, so the named tests and public-path commands above are the primary acceptance evidence rather than relying on an incomplete review invocation.

## Final assessment

- Local PLAN_V7.1 implementation and public/integration acceptance paths: **verified/workflow-validated**.
- Whole-result feedback loop: **closed for locally executable requirements**, because each local requirement is mapped to an observed command/test result above.
- External VM/signing/reboot/multiprocess gates: **acceptance-blocked and intentionally not overstated**.
- Secret values were not copied into this log.
