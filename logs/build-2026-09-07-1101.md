## Goal

프로젝트를 빌드하고, 검토된 라우팅 및 스트리밍 타임아웃 수정 사항을 커밋하며, main 브랜치에 푸시합니다.

## Actions

- 모든 Go 패키지와 명령 패키지를 성공적으로 빌드했습니다.
- 스테이징 전에 생성된 `fmr.exe` 및 `fmr-package.exe` 바이너리를 제거했습니다.
- 직접 테스트와 함께 구현을 페어링하여 두 개의 원자적 커밋을 생성했습니다.
- 커밋을 `origin/main`에 푸시했습니다.
- 로컬 `main`과 `origin/main`이 동기화되어 있음을 확인했습니다.

## Files Changed

- `internal/gateway/gateway.go`: 결정론적 자동 모델 선택.
- `internal/gateway/gateway_test.go`: 선택 회귀 커버리지.
- `internal/providers/opencode/provider.go`: 스트림 안전 요청 범위 타임아웃 동작.
- `internal/providers/opencode/chat.go`: 타임아웃 인식 완료 처리.
- `internal/providers/opencode/opencode_test.go`: 타임아웃 및 스트림 컨텍스트 회귀 커버리지.

## Result

- 성공: 빌드, 커밋, 푸시 완료.
- 커밋: `bbc3bb8 fix(gateway): make automatic model selection deterministic`; `1a403cb fix(opencode): preserve long-running streams`.
- `origin/main`이 로컬 `main`과 동기화됨.

## Next Steps

- 남은 코드 변경 사항 없음. 프로세스 로그와 생성된 `.codegraph` 인덱스는 의도적으로 추적되지 않음.