## Goal

Correct health concurrency, latency preference, and dynamic probe behavior before runtime integration.

## Actions

- Replaced busy-wait provider slots with context-aware, notification-based idempotent leases.
- Changed probes to use route provider identity, bounded probe contexts, and dynamic snapshot loading with jitter.
- Preferred ProbeTTFT for routing and added bool default regression coverage.
- Corrected scheduler lifecycle using run IDs and completion waiting in Stop.
- Treated semantic-less probe completion as a route failure while preserving parent-cancellation behavior.
- Ran full race tests, vet, and build manually with the user-local WinLibs environment; all passed.

## Files Changed

- `internal/health/health.go`, `internal/health/health_test.go`: cancelable provider slots and tests.
- `internal/latency/latency.go`, `internal/latency/latency_test.go`: ProbeTTFT priority.
- `internal/probe/probe.go`: dynamic snapshots, jitter, timeout, provider slots, and lifecycle corrections.
- `internal/providers/opencode/opencode_test.go`: boolOr behavior coverage.

## Result

- Success: local full `go test ./... -race`, `go vet ./...`, and `go build ./cmd/Free-Model-Router` passed with `C:\Users\USER\AppData\Local\Programs\WinLibs\16.2-ucrt-r1\mingw64\bin` on process PATH.
- Tester subagent's final race command lacked that PATH and failed only on missing GCC; vet/build passed. This is an environment invocation limitation, not a code failure.

## Next Steps

- Begin PLAN_V7.1 runtime composition and Phase 6.5 integration work: app-owned registry/snapshot state, gateway candidate filtering, and end-to-end mock OpenCode tests.